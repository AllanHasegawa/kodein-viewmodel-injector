class Version {
    int major = 0
    int minor = 3
    int patch = 0

    int code() {
        patch + minor * 1_000 + major * 100_000
    }

    String name() {
        "$major.$minor.$patch"
    }
}
ext {
    version = new Version()
}