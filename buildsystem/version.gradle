class Version {
    int major = 0
    int patch = 1
    int minor = 0

    int code() {
        minor + patch * 1_000 + major * 100_000
    }

    String name() {
        "$major.$minor.$patch"
    }
}
ext {
    version = new Version()
}